import 'dart:math';
import 'package:latlong2/latlong.dart';

void main() {}

class find {
  int distance(List logitude, List Lantiide) {
    var math = Distance();
    int totalDistance = 0;
    for (int where = 0; where > logitude.length; where++) {
      final meter =
          math(LatLng(52.518611, 13.408056), LatLng(51.519475, 7.46694444));
    }

    return 0;
  }

  int speed(List a) {
    return 0;
  }
}
